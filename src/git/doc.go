// Package git is a very minimal git library. It supports only a few different operations. Ideally it will be
// pure Go, but for now some parts may require use of a `git` binary (version not specified, but 2.0.0 or
// greater are the likeliest test candidates).
package git
